#![cfg(test)]

/// take an input, turn it into an output, and then compare it to the expected output
/// also compile the output to see if clang screws up
fn check(i: &str, expected_o: &str) {
    unimplemented!()
}

#[test]
fn it_works() {}
